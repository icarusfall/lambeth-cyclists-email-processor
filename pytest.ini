[pytest]
# Only collect the real unit tests. The helpers in scripts/ talk to live APIs
# (and check_notion.py creates a real Notion page), so they must never be
# picked up by a bare `pytest`.
testpaths = tests
