# Google APIs
google-auth==2.57.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2
google-api-python-client==2.199.0

# Claude API
# 1.x requires Python >=3.10 and moves the HTTP layer to httpx2 (pulled in
# transitively). Sampling params were removed from the 1.x call signatures —
# see services/claude_service.py.
anthropic>=1,<2

# Notion API
# PINNED BELOW 3.0 DELIBERATELY. notion-client v3 replaces databases.query()
# with the data_sources API; services/notion_service.py still uses the v2
# surface. Bumping this is a code migration, not a version bump.
# (The MCP server repo is already on v3.)
notion-client>=2.2,<3

# HTTP requests
requests==2.34.2

# Email sending (Resend API)
resend==2.42.0

# Attachment processing
pdfplumber==0.11.10
python-docx==1.2.0
# pandas 3.x requires Python >=3.11; local dev is on 3.10 while Railway is on
# 3.12. Held at the newest 2.x so both environments resolve identically.
pandas==2.3.3
openpyxl==3.1.5
tabulate==0.10.0

# Data validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Environment variables
python-dotenv==1.2.3

# Error monitoring (optional)
sentry-sdk==2.68.1

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1

# Async support
aiohttp==3.14.3
